<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
  <mapper namespace="com.DDinside.desktop.mapper.RegisterMapper">
  
  <insert id="insertMember" parameterType="hashmap">
  insert into memberlist1(num, id, pass, name, email) 
  values(memberlist1_seq.nextval,#{memberVO.id},#{memberVO.pass},#{memberVO.name},#{memberVO.email})
  </insert>
  
  </mapper>